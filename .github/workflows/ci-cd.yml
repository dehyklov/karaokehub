name: CI/CD Demo

on:
  push:
    branches:
      - main

jobs:
  demo:
    name: Demo pipeline
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: CI step
        run: echo "CI step passed"

      - name: Build step
        run: echo "Build passed"

      - name: Deploy step
        run: echo "Deploy passed"
